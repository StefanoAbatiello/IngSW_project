package it.polimi.ingsw.model.cards;

import it.polimi.ingsw.model.Player;
import it.polimi.ingsw.model.Resource;

public class LeadAbilityProduction {

    /*public boolean activateAbilityOn(Player p){
        if(!p.isProductionAbility()) {
            p.setProductionAbility(true);
            return true;
        }else
            return false;
    }

   /* public boolean useAbility(LeadCard leadCard, Player p){
        Resource answer= Resource.SHIELD;
        if(checkDepots(leadCard.getLeadCardRes())){
                /**chiedo al giocatore cosa vuole e lo metto nel forziere
                fillStrongBox(answer);
                p.updateFaithMarker();
                return true;
            }else
                return false;
        }*/
    }

